<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- The in-app updater (feature/update) only. Restricted on the Play Store; the whole feature lives in that package. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- QR sign-in's scanner (zxing-android-embedded's CaptureActivity asks for this itself). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Not required: a phone with no camera still signs in with a password; the scan button hides itself (PackageManager.FEATURE_CAMERA_ANY). -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!--
      No cloud backup: the mirror is rebuilt from the server on first sign-in, and the token is
      wrapped by a Keystore key that never leaves this device, so a restored copy could use neither.
    -->
    <application
        android:name=".LogbApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Logb"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="33">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- A photo or a PDF shared from anywhere lands in an entry form. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
                <data android:mimeType="application/pdf" />
            </intent-filter>
            <!--
              A `logb://pair` link, scanned by another device or opened from a browser. No
              android:autoVerify: a custom scheme can't be domain-verified, so this never claims
              to be an App Link. ShareInbox always asks for confirmation before it is acted on;
              see PairingConfirmViewModel.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="logb" android:host="pair" />
            </intent-filter>
            <meta-data android:name="android.app.shortcuts" android:resource="@xml/shortcuts" />
        </activity>

        <!-- Where the platform installer reports the outcome of an update it was handed. -->
        <receiver
            android:name=".feature.update.InstallResultReceiver"
            android:exported="false" />

        <!-- Done / Snooze tapped on a reminder notification. -->
        <receiver
            android:name=".core.notify.ReminderActionReceiver"
            android:exported="false" />

        <!-- Home-screen widget: due reminders, one tap to mark one done. -->
        <receiver
            android:name=".feature.widget.DueWidgetReceiver"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/due_widget_info" />
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- WorkManager is configured by LogbApp (Hilt worker factory), not by the initializer. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>
</manifest>
